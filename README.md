# Culqi Pago Spring Boot

Simple web service para pagos con Culqi.

Usando Spring Boot.

## Configuración
- Copiar `sample-application.properties` como `application.properties`
    - `server.port`
    - `culqi.sk`